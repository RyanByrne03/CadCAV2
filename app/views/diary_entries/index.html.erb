<!DOCTYPE html>
<html>
  <head>
    <title>Diary Entries</title>
    <!-- Linking Bootstrap CSS for styling -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css">
  </head>
  <body>
    <!-- Navbar section for navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <!-- Brand logo or title -->
        <a class="navbar-brand" href="#">Diary App</a>
        <!-- Button for toggling the navigation menu in smaller screens -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <!-- Collapsible navigation menu -->
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <!-- Link to the home page displaying all diary entries -->
            <li class="nav-item">
              <%= link_to "Home", diary_entries_path, class: "nav-link" %>
            </li>
            <!-- Link to create a new diary entry -->
            <li class="nav-item">
              <%= link_to "New Diary Entry", new_diary_entry_path, class: "nav-link" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Main container for content -->
    <div class="container mt-4">
      <!-- Notification message, if any -->
      <p style="color: green"><%= notice %></p>

      <!-- Setting the title of the page dynamically -->
      <% content_for :title, "Diary entries" %>

      <h1>Welcome to the app!</h1>

      <!-- Section displaying all diary entries -->
      <div class="container mt-4">
        <div class="row">
          <% @diary_entries.each do |diary_entry| %>
            <div class="col-md-4">
              <div class="custom-card">
                <div class="custom-card-header">
                  <h5><%= diary_entry.title %></h5>
                </div>
                <div class="custom-card-body">
                  <p><%= truncate(diary_entry.content, length: 100) %></p>
                  <p class="text-muted"><%= diary_entry.date %></p>
                  <%= link_to "Show this diary entry", diary_entry, class: "btn btn-primary btn-sm" %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>

    <!-- Linking Bootstrap JavaScript for interactive components -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>